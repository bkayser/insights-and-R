# These are all the packages used in different scripts.  You may have to install
# them before importing the library the first time, using the R call 
# install.package('packagename')
#
library(ggplot2)
library(plyr)
library(dplyr)
library(igraph)
library(datasets)

source('insights_api.R')
